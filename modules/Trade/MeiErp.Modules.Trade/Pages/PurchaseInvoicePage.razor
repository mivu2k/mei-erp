@page "/purchase/invoices/new"
@page "/purchase/invoices/{Id:int}"
@attribute [Authorize(Policy = PurchaseModule.InvoicesManage)]
@rendermode InteractiveServer

@* The dialog and this page render the same editor; only the frame differs. *@
<InvoiceEditor AsDialog="false" IsPurchase="true" InvoiceId="Id" />

@code { [Parameter] public int? Id { get; set; } }
