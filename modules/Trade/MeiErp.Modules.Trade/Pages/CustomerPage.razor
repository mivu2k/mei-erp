@page "/sales/customers/new"
@page "/sales/customers/{Id:int}"
@attribute [Authorize(Policy = SalesModule.CustomersManage)]
@rendermode InteractiveServer

@* The dialog and this page render the same editor; only the frame differs. *@
<PartyEditor AsDialog="false" PartyId="Id" DefaultSupplier="false" />

@code { [Parameter] public int? Id { get; set; } }
