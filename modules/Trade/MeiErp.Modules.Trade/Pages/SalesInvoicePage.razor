@page "/sales/invoices/new"
@page "/sales/invoices/{Id:int}"
@attribute [Authorize(Policy = SalesModule.InvoicesManage)]
@rendermode InteractiveServer

@* The dialog and this page render the same editor; only the frame differs. *@
<InvoiceEditor AsDialog="false" IsPurchase="false" InvoiceId="Id" />

@code { [Parameter] public int? Id { get; set; } }
