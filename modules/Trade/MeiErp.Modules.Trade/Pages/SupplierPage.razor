@page "/purchase/suppliers/new"
@page "/purchase/suppliers/{Id:int}"
@attribute [Authorize(Policy = PurchaseModule.SuppliersManage)]
@rendermode InteractiveServer

@* The dialog and this page render the same editor; only the frame differs. *@
<PartyEditor AsDialog="false" PartyId="Id" DefaultSupplier="true" />

@code { [Parameter] public int? Id { get; set; } }
