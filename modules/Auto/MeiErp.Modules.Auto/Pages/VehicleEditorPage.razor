@page "/auto/vehicles/new"
@page "/auto/vehicles/{Id:int}/edit"
@attribute [Authorize(Policy = AutoModule.VehiclesManage)]
@rendermode InteractiveServer

@* The dialog and this page render the same editor; only the frame differs. *@
<VehicleEditor AsDialog="false" VehicleId="Id" />

@code {
    [Parameter] public int? Id { get; set; }
}
