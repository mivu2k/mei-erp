@page "/repair/jobs/new"
@page "/repair/jobs/{Id:int}/edit"
@attribute [Authorize(Policy = RepairModule.JobsManage)]
@rendermode InteractiveServer

@* The dialog and this page render the same editor; only the frame differs. *@
<JobEditor AsDialog="false" JobId="Id" />

@code {
    [Parameter] public int? Id { get; set; }
}
