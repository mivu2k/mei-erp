@page "/gatepass/passes/new"
@page "/gatepass/passes/{Id:int}/edit"
@attribute [Authorize(Policy = GatePassModule.PassesRaise)]
@rendermode InteractiveServer

@* The dialog and this page render the same editor; only the frame differs. *@
<PassEditor AsDialog="false" PassId="Id" />

@code {
    [Parameter] public int? Id { get; set; }
}
