@page "/finance/third-parties/new"
@page "/finance/third-parties/{Id:int}/edit"
@attribute [Authorize(Policy = FinanceModule.PartiesManage)]
@rendermode InteractiveServer

@* The dialog and this page render the same editor; only the frame differs.
   "/edit" avoids colliding with PartyDetail's "/finance/third-parties/{id}" statement route. *@
<PartyEditor AsDialog="false" PartyId="Id" />

@code {
    [Parameter] public int? Id { get; set; }
}
