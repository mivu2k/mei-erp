@page "/finance/utilities/connections/new"
@attribute [Authorize(Policy = FinanceModule.UtilitiesManage)]
@rendermode InteractiveServer

@* The dialog and this page render the same editor; only the frame differs.
   No edit route: the editor only ever creates a connection. *@
<ConnectionEditor AsDialog="false" />
