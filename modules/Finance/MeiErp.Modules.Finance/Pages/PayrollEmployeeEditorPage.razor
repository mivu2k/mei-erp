@page "/finance/payroll/employees/new"
@page "/finance/payroll/employees/{Id:int}"
@attribute [Authorize(Policy = FinanceModule.PayrollManage)]
@rendermode InteractiveServer

@* The dialog and this page render the same editor; only the frame differs. *@
<PayrollEmployeeEditor AsDialog="false" EmployeeId="Id" />

@code {
    [Parameter] public int? Id { get; set; }
}
